# unix-shell
A POSIX-compliant Unix shell written in C, featuring a CFG-based command parser, pipelines, I/O redirection, background execution, and job control. Built using fork, pipe, dup2, and signal handling (SIGINT, SIGTSTP) with optimized process synchronization.
